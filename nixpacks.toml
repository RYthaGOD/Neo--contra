[phases.setup]
nixPkgs = ["nodejs_22", "python3", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm install --prod=false --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm start"

# cache-bust: 20260616013209

